"use strict";

const baseUrl = "http://localhost/bpm/kazan/";
const workspace = 0;
const startPageUrl = `${baseUrl}${workspace}/Nui/ViewModule.aspx#`;

module.exports = {
	baseUrl,
	workspace,
	startPageUrl
};